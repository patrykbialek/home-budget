{
  "rules": {
    "workspaces": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        "transactions": {
          ".indexOn": ["date", "category_date"]
        }
      }
    }
  }
}
